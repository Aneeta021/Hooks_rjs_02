//1st_Atione
const incNum = () => {
  return {
    type: "Increment",
  };
};

//2nd_Ation
const decNum = () => {
  return {
    type: "Decrement",
  };
};

export { incNum, decNum };

//all Action add here..............export in 2 way i.e one is all actions export in bottom like as it 
// 2nd is export with the action seprate seprate one by one 
